{
  "buildCommand": "if [ -d \"flutter\" ]; then echo \"Flutter already installed\"; else git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'No install needed'"
}
